export const metadata = {
  title: 'All Pending Winners - Go Win',
  description: 'Find all pending winners of "Go Win" Lottery',
};

const PendingWinners = () => {
  return <div>Winners</div>;
};

export default PendingWinners;
